# Synthetic analysis

Across ten measured passes, baseline median latency is approximately 101.4 ms and candidate median latency is approximately 87.9 ms.

Relative reduction = (101.4 - 87.9) / 101.4 ≈ 13.3%.

Observed parse errors: baseline 0, candidate 0.

Under the frozen outcome rule, the reported result therefore exceeds the registered 12% latency-reduction threshold without increasing errors.
