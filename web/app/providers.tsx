"use client";
import {ResearcherSession} from "@/identity/researcher-session";
export function Providers({children}:{children:React.ReactNode}){return <ResearcherSession>{children}</ResearcherSession>}
