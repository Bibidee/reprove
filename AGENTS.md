# Agent instructions

Read `MEGA_PROMPT_FOR_AGENT.md` before making release changes.

Hard constraints: Studionet 61999 only; preserve the three-contract trust boundaries; preserve the five public routes; preserve the clean-room research frontend; never present ACCEPTED as final; never make `INCONCLUSIVE` pay; never let model output select reward amounts.
